# porcentagem / desconto (5%)
price = float(input('Product price: R$'))
discount = price * 0.05
print('The product costs: R${}'
      '\nWith 5% of discount is costs: R${:.2f}'.format(price, price - discount))
