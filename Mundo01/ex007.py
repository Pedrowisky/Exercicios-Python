# media de duas notas de um aluno
nota1 = float(input('Student first note: '))
nota2 = float(input('Student second note: '))
s = nota1 + nota2
print('The average between {:.2} and {:.2} is {:.2}'.format(nota1, nota2, s/2))
